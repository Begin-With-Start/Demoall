<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/btn_from_gallery"
        style="@style/Widget.AppCompat.Button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="pickFromGallery"
        android:text="从相册选取" />

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:id="@+id/old_image_id"
        android:scaleType="centerInside"
        android:layout_below="@+id/btn_from_gallery"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/oldimage_txt"
        android:text="旧的照片大小"
        android:layout_below="@+id/old_image_id"
        />

    <RelativeLayout
        android:layout_below="@+id/oldimage_txt"
        android:layout_width="match_parent"
        android:layout_weight="1"
        android:layout_height="0dp">

        <ImageView
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:scaleType="centerInside"
            android:id="@+id/new_image_id"
            />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/newimage_txt"
            android:text="新的照片大小"
            android:layout_below="@+id/new_image_id"
            />
    </RelativeLayout>


</LinearLayout>
